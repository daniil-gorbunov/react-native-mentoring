import React from 'react';
import {SafeAreaView} from 'react-native';

import {Main} from './screens';

export const App = () => {
  return (
    <SafeAreaView>
      <Main />
    </SafeAreaView>
  );
};
